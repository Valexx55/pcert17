
//M脫DULOS

1) Para poder empaquetar las librer铆as en un m贸dulo primero hay que hacerlas modulares


2) No es recomendable, pero se puede decir por linea de comandos qu茅 paquetes 
   un m贸dulo exporta -mejor en el module-info.java-
   
3) javac --module-source-path src -d out src/foo.bar/*/*.java 

   隆隆INCORRECTO!! --> no puedes usar comod铆n (*) en los directorios

4) javac --module-source-path o --module debe especificar el directorio de 
   salida con -d
   
5) module-path me permite indicar el path para ejecutar ese m贸dulo (donde est谩n 
   las clases necesarias /referenciadas en el entorno ) para la ejecuci贸n del m贸dulo
   
   Tb puedo a帽adir m贸dulos ya compilados a entornos de ejecuci贸n con esa opci贸n
 
	5.1) Tb con --module-source-path puedo a帽adir todo el c贸digo depedendiente 
	y as铆 compilar el m贸dulo con todo lo necesario, sin necesidad de usar el 
	comando anterior
	
6) si un M贸dulo usa otro m贸dulo pero s贸lo temporalmente, puede indicarlo 
   por l铆nea de comandos
   
7) Un m贸dulo exporta s贸lo paquetes

8) El m贸dulo java.base es impl铆citamente importado en cada m贸dulo que yo cree

9) El paquete java.lang pertenece al m贸dulo java.base

10) Para modulizar un jar que ya existe, basta con a帽adir el fichero module-info.java
    debidamente
    
11) --module-path para las clases compiladas / jar
    --module-source-path para el c贸digo fuente en modulos

12) Si usas module-info, debes importar con require modulos como java.sql
	que entiendo est谩n fuera de java.base- entiendo-
	si no, no hace falta (accedes a todo)
	
13) Si tienes un JAR ordinario y en su paquete por defecto una clase
    esa clase no se va a poder usar como un m骴ulo autom醫ico (requires en modulo con nombre)
    
14) REPASAR el nombrado de m骴ulos autom醫icos
  
 ---------------------------------------------------------------------------
 ---------------------------------------------------------------------------
   
//SERVICE LOADER
 
 1) ServiceLoader implementa Iterable!!
 
 2) Dado este module-info
	
	module foo.filter
		{   
		 requires api;   
		 provides api.Filter with foo.DoNothingFilter; 
		}


	Debe haber una clase DoNothingFilter que o bien implemente Filter
    -e implmente todos los m茅todos y adem谩s un constructor por defecto con 0 argumentos-

	O bien la clase DoNothingFilter tiene un m茅todo 
		
		public static provider Filter
	
	que devuelve una instanacia de Filter que incluye 
	la implementaci贸n de toda la interfaz

3) ServiceLoader tiene el m茅todo iterator() pero No services()
 